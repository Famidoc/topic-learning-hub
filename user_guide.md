# TopicHub 萬用主題學習手冊建構器 - 使用手冊 📖

歡迎使用 **TopicHub**！這是一個完全運行在您本機瀏覽器中的「萬用主題學習手冊建構器」。為了保護您的隱私，本程式**無伺服器中轉**，所有產生的學習手冊皆直接儲存於您的本機或**個人 Google 雲端硬碟**。

本手冊將以最白話、無程式碼的方式，引導您完成各項設定。

---

## 🔑 第一部分：取得並設定 Gemini AI 金鑰（產生手冊核心）

TopicHub 需要使用 Google 的 AI 引擎來為您規劃學習路徑。請按照以下步驟免費申請您的個人 AI 金鑰：

1. 🌐 **前往申請網站**：
   * 用瀏覽器開啟 [Google AI Studio 官網](https://aistudio.google.com/)。
   * 使用您的 **Google 帳號** 進行登入。
2. 🖱️ **建立金鑰**：
   * 點選頁面左上角的 **"Get API Key"**（獲取 API 金鑰）按鈕。
   * 點選 **"Create API Key"**（建立 API 金鑰）。
3. 📋 **複製金鑰**：
   * 在彈出的視窗中，點選您的專案名稱，然後點擊 **"Copy"**。
   * 金鑰格式通常為一串長英數組合（例如：`AIzaSy...`）。
4. ⚙️ **貼回 TopicHub**：
   * 開啟您的 TopicHub 網頁，點選側邊欄（或手機選單）的 ⚙️ **設定**。
   * 在 **Gemini API 金鑰** 欄位中，**貼上**剛剛複製的金鑰。
   * 點擊最下方的 **"儲存配置設定"**。

> [!NOTE]
> 💡 **小貼士**：這個金鑰只會儲存在您手機或電腦的瀏覽器本機快取中，絕不會外洩，且在合理使用範圍內是完全免費的。

---

## 💾 第二部分：取得並設定 Google 雲端硬碟連線（雲端備份）

如果您希望手冊能跨裝置同步，並且永久保存在雲端，您需要建立一個 Google 雲端連接通道。本步驟稍微需要一點耐心，請按圖索驥：

1. 🌐 **登入 Google 開發者後台**：
   * 開啟 [Google Cloud Console 控制台](https://console.cloud.google.com/)，登入您的 Google 帳號。
2. 📁 **建立專案**：
   * 點選頂部導覽列的專案下拉選單，點選右上角的 **"新建專案"** (New Project)。
   * 輸入專案名稱（例如：`TopicHub`），然後點擊 **"建立"**。
3. 🔓 **啟用雲端硬碟權限**：
   * 在左側選單中，點選 **"API 和服務"** (APIs & Services) ➔ **"庫"** (Library)。
   * 在搜尋欄中搜尋 **`Google Drive API`**。
   * 點擊進入並點選 **"啟用"** (Enable)。
4. 📝 **配置同意畫面（OAuth Consent Screen）**：
   * 點選左側選單的 **"OAuth 同意畫面"** (OAuth consent screen)。
   * 用戶類型選擇 **"外部"** (External)，點擊 **"建立"**。
   * 填寫必填欄位：
     * **應用程式名稱**：`TopicHub`
     * **開發人員與使用者支援電子郵件**：填寫您的 Google 信箱。
   * 一路點擊 **"儲存並繼續"** 直到完成。
   * ⚠️ **關鍵步驟**：在同意畫面設定頁面的 "發布狀態"，將其從「測試中」改為 **"發布應用程式"** (Publish App)，確保連線不會失效。
5. 🔑 **建立連線 ID（Client ID）**：
   * 點選左側選單的 **"憑證"** (Credentials)。
   * 點選頂部的 **"+ 建立憑證"** ➔ **"OAuth 用戶端 ID"** (OAuth client ID)。
   * 應用程式類型選擇 **"網頁應用程式"** (Web application)。
   * 在 **"已授權的 JavaScript 來源"** (Authorized JavaScript origins) 區塊下，點擊 **"+ 新增 URI"**，並輸入您 TopicHub 網站的網址。
     * *例如*：`https://您的帳號.github.io`
   * 點擊 **"建立"**，系統將會顯示您的 **"用戶端 ID"**（格式為 `xxxxxx.apps.googleusercontent.com`）。
6. ⚙️ **貼回 TopicHub**：
   * 複製該用戶端 ID。
   * 開啟 TopicHub ➔ ⚙️ **設定** ➔ 貼到 **Google 雲端硬碟 API 設定** ➔ 點擊 **儲存配置設定**。
   * 返回首頁，點選頂部的 **"連線 Google Drive"** 完成帳號授權！

---

## 📱 第三部分：如何在手機桌面上安裝 TopicHub（PWA 安裝）

TopicHub 支援漸進式網頁應用（PWA）技術。安裝後，它會像一個獨立的原生 App 一樣顯示在您的手機桌面上，沒有瀏覽器的網址列，開啟速度更快。

### 🍏 蘋果手機 (iOS / Safari 瀏覽器)
1. 🌐 使用 iPhone 內建的 **Safari 瀏覽器** 開啟您的 TopicHub 網址。
2. 📤 點擊螢幕底部工具列正中間的 **「分享」** 按鈕（一個帶有向上箭頭的方塊）。
3. 📜 在彈出的選單中向下滾動，點選 **「加入主畫面」** (Add to Home Screen)。
4. ✍️ 確認名稱（預設為 `TopicHub`），然後點擊右上角的 **「新增」**。
5. 🎉 安裝完成！現在您可以直接從手機桌面點擊圖標開啟。

### 🤖 安卓手機 (Android / Chrome 瀏覽器)
1. 🌐 使用 **Google Chrome 瀏覽器** 開啟您的 TopicHub 網址。
2. 🔔 **偵測提示**：瀏覽器通常會在畫面底部自動跳出 **「將 TopicHub 新增至主畫面」** 的橫幅提示，直接點選即可。
3. 💬 **手動安裝**：如果沒有自動彈出提示，請點擊 Chrome 右上角的 **「三個點（選單）」** 按鈕。
4. 📥 點選選單中的 **「安裝應用程式」** (Install App) 或 **「加入主畫面」**。
5. 🎉 安裝完成！TopicHub 已成為您桌面上的獨立應用程式。
